<div class="padding-top-md">
    <div class="row">
        <div class="col-sm-4 col-sm-push-8">
            <h4>Your Account</h4>
            <p>We're all in this together, and we would love to get to know you a little bit better. Where do you work? Which class did you do with Codaisseur? Take it away!</p>
        </div>



        <div class="col-sm-8 col-sm-pull-4 content-wrapper">
<%= form_for @profile do |f| %>
       <table class="table table-profile">
          <tbody>
            <tr>
              <% if @profile.user.present? %>
                  <% @user = @profile.user %>
              <% else %>
                  <% @user = current_user %>
              <% end %>
              <td><%= render partial: "layouts/avatar", locals: { user: @user, size: :large } %></td>
              <td><%= f.file_field :avatar %></td>
            </tr>
            <tr>
              <td><%= f.label :first_name %></td>
              <td><%= f.text_field :first_name, required: true, class: "form-control" %></td>
            </tr>
            <tr>
              <td><%= f.label :last_name %></td>
              <td><%= f.text_field :last_name, required: true, class: "form-control" %></td>
            </tr>
            <% if @user.is_student? %>
              <tr>
                <td><%= f.label :course %></td>
                <td><%= f.collection_select :course_id, Course.all, :id, :name, { include_blank: true }, class: "form-control" %></td>
              </tr>
            <% end %>
            <tr>
              <td><%= f.label 'About you' %></td>
              <td><%= f.text_area :bio, size: '30x5', class: "form-control" %></td>
            </tr>
            <tr>
              <td><%= f.label 'Github username' %></td>
              <td><%= f.text_field :github, class: "form-control" %></td>
            </tr>
            <tr>
              <td><%= f.label 'Twitter username' %></td>
              <td><%= f.text_field :twitter, class: "form-control" %></td>
            </tr>
            <tr>
              <td><%= f.label 'Website URL' %></td>
              <td><%= f.text_field :website, class: "form-control" %></td>
            </tr>
            <tr>
              <td colspan="2" class="actions btn-submit-wrapper">
               <%= link_to "Cancel", @profile, class: "btn btn-default" %>
                <%= f.submit (@profile.new_record? ? "Create" : "Update"), class: "btn btn-codaisseur" %>
              </td>
            </tr>
          </tbody>
        </table>
 <% end %>
        </div>
    </div>
</div>
